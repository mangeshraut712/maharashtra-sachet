# Region packs

The running Worker uses **`web/region.json`** so the browser and the API share one bbox and centroid table.

This directory is documentation for forks: copy `web/region.json`, change `id` / `bbox` / `districtCentroids`, and point collectors at your CAP sources. See [docs/FORKING.md](../docs/FORKING.md).
